"""Финансовый контур в разрезе одной компании.

Данные берутся из той же PostgreSQL, что выбрана заголовком ``X-Company-Slug`` (при
``DATABASE_SEPARATE_DBS=true`` — отдельная база на компанию).

- **Оплаты (приток):** сущности проектов / ``received-payments`` (как сейчас).
- **Расходы по команде в P&L:** ``employee_payment_records`` по пользователям с ролью ``employee``
  (включая самоввод), минус поле ``budget_amount`` (проходной бюджет клиента — не расход компании);
  USD в зарплатном фонде переводятся в сумы курсом ``usd_to_uzs_rate`` из ДДС за месяц колонки.
  Раздел «Расходы» в UI показывает записи администратора; в итогах — сумма ``amount − budget_amount``.
- **Projects Cost:** ``GET /api/finance/projects-cost`` — ``payments`` + ``payment_months``.
- **P&L:** ``GET /api/finance/pl?year=…`` — проекты (график) + строки ``cash_flow_entries`` (приход/расход ДДС по месяцам).
- **ДДС:** ``/api/finance/cash-flow/*`` — шаблоны месяца (зарплаты, админ), ручные строки, формы оплаты и категории;
  агрегаты попадают в P&L (зарплата, офис, бухгалтерия, прочее).

Всё в БД выбранной компании (slug), без смешивания организаций.
"""
