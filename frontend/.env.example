# Скопируй в .env.local и подставь порт, на котором запущен EnterDebt API.
# Если на :8000 уже другой сервис (другой проект) — запускай backend на 8001 и укажи его здесь.
BACKEND_URL=http://127.0.0.1:8001
